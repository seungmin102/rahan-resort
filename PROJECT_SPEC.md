# 라한호텔 휴양시설 신청 시스템 — 프로젝트 스펙 (인계용)

## 1. 프로젝트 개요
- **목적**: 사내 복지 이벤트 "휴가 사용 활성화 이벤트"의 일환으로, 직원들이 라한호텔 휴양시설 이용을 온라인으로 신청하고, 관리자가 신청 현황을 확인·승인/거절 처리 후 호텔 예약실과 소통(예약번호 기록)하는 시스템.
- **사용 대상**: 사내 직원(신청자), 복지 담당자(관리자) 1인.
- **신청 접수 기간**: 2026년 10월 1일 ~ 12월 31일.
- **이용 가능 요일**: 일~목요일만 (금·토요일은 이용 불가로 신청 자체가 막힘).

## 2. 현재 아키텍처
정적 프론트엔드(HTML/CSS/JS) + 구글 앱스 스크립트(백엔드 API) + 구글 시트(데이터 저장소) 구조.
Claude 아티팩트(claude.ai) 기반이 아니라, **외부에서 로그인 없이 접근 가능한 구조**로 전환 완료.

```
[직원 브라우저] ──fetch(GET/POST)──▶ [Google Apps Script Web App] ──▶ [Google Sheets]
[관리자 브라우저] ──fetch(GET/POST)──▶ (동일 API 엔드포인트)
```

### 2.1 프론트엔드 (정적 파일, GitHub Pages에 호스팅 중)
- 저장소: `https://github.com/seungmin102/rahan-resort`
- 배포 주소: `https://seungmin102.github.io/rahan-resort/`
  - 직원용 신청 화면: `.../apply.html`
  - 관리자용 화면: `.../admin.html`
  - QR코드 안내 페이지: `.../qr-apply.html` (선택, apply.html 링크를 QR로 보여줌)
- 두 HTML 파일 모두 완전히 독립적인 정적 파일이며, 파일 상단 `<script>` 블록의 `CONFIG.API_URL` 값으로 백엔드와 통신함.
- 실시간 push 없음 (Google Apps Script는 웹소켓 미지원). 대신:
  - 관리자 화면: 20초 주기 자동 폴링 + 수동 새로고침 버튼.
  - 신청 완료 화면: 10초 주기로 본인 신청 건의 상태만 폴링해서 뱃지 갱신.

### 2.2 백엔드 (Google Apps Script)
- 소스 파일: `Code.gs` (구글 시트에 바인딩된 Apps Script 프로젝트).
- 배포 방식: **웹 앱(Web App)**으로 배포.
  - 실행 계정(Execute as): **나(관리자 본인 계정)** — 반드시 이 설정이어야 방문자가 로그인 없이도 시트에 접근 가능.
  - 액세스 권한(Who has access): **전체(Anyone)** — 로그인 불필요.
  - 배포 URL 예시: `https://script.google.com/macros/s/{DEPLOYMENT_ID}/exec`
  - **주의**: "새 배포"가 아니라 "배포 관리 → 기존 배포 편집"으로 재배포해야 URL이 유지됨.

#### Code.gs 수정 후 재배포 절차 (매번 이대로)
코드를 고쳐도 **재배포하지 않으면 웹 앱에는 반영되지 않는다.** 편집기의 코드와 배포된 코드는 별개다.

1. 구글 시트 → 확장 프로그램 → Apps Script
2. 기존 코드 전체 선택(Ctrl+A) 후 새 `Code.gs` 내용으로 교체
3. **저장 (Ctrl+S).** 이걸 빼먹으면 다음 단계에서 예전 코드가 버전으로 굳는다.
   상단 파일명 옆의 수정됨 표시가 사라져야 저장된 것.
4. 우측 상단 **배포 → 배포 관리**
5. 목록에서 **현재 쓰는 배포**를 고른다. 웹 앱 URL이 `apply.html`/`admin.html` 의
   `CONFIG.API_URL` 과 같은 것이어야 한다(배포가 여러 개면 여기서 자주 틀린다).
6. 우측 상단 **연필(✏️) 아이콘** 클릭
7. **버전 드롭다운을 "새 버전"으로 변경** ← 기본값이 현재 버전이라 그냥 두면 아무것도 안 바뀐다.
   이 단계가 재배포 실패의 대부분이다.
8. **배포** 버튼 클릭
9. 확인: 브라우저에서 `<웹앱URL>?action=adminData` 를 열어
   `{"ok":true,"data":{"applications":[...],"blocked":[...]}}` 가 나오면 성공.
   `{"ok":false,"error":"unknown_action"}` 이면 3·7단계를 다시 확인할 것.
- CORS 처리: POST 요청은 `Content-Type: text/plain;charset=utf-8`로 보내 브라우저의 CORS preflight(OPTIONS)를 우회함 (Apps Script가 preflight를 제대로 지원하지 않기 때문). 서버 측에서는 `e.postData.contents`를 직접 JSON.parse해서 처리.

### 2.3 데이터 저장 (Google Sheets)
Apps Script가 최초 요청 시 아래 두 시트를 자동 생성함.

**`Applications` 시트** (헤더 순서 고정, 코드의 `APPS_HEADERS` 배열과 일치해야 함):
| 열 | 필드명 | 설명 |
|---|---|---|
| A | id | UUID, 문서 고유 식별자 |
| B | appNo | 4자리 숫자 신청번호 (1000~9999, 랜덤+중복검사) |
| C | name | 신청자 이름 |
| D | empid | 사번 (숫자 6자리) |
| E | branch | 지점 (경주/전주/포항/목포/울산) |
| F | roomtype | 룸타입 (디럭스 더블/디럭스 트윈) |
| G | date | 이용일 (YYYY-MM-DD 문자열) |
| H | nights | 박수 (1박2일/2박3일) |
| I | people | 인원 (항상 2 고정) |
| J | memo | 요청사항 (선택 입력) |
| K | status | 대기/승인/거절 |
| L | reservationNo | 예약번호 (관리자가 호텔 회신 후 입력) |
| M | submittedAt | 제출 시각 (ISO 8601 문자열) |

**`BlockedDates` 시트**:
| 열 | 필드명 |
|---|---|
| A | date (YYYY-MM-DD) |
| B | blockedAt (ISO 8601) |

**⚠️ 중요 — 자동 서식 변환 문제**: 구글 시트는 `appNo`(D열 사번), `date`(G열), `appNo`(B열)처럼 숫자/날짜처럼 보이는 문자열을 자동으로 숫자·날짜 형식으로 바꿔버리는 습성이 있음. 이를 막기 위해 **B, D, G열(Applications) / A열(BlockedDates)을 미리 '서식 > 숫자 > 일반 텍스트'로 지정**해두는 것을 강력히 권장. 코드에서도 `formatDateValue()` 헬퍼로 방어적으로 문자열 변환하고 있으나, 근본적으로는 시트 서식을 맞춰두는 편이 안전하고 빠름.

## 3. API 명세

### 3.1 GET 요청 (`doGet`)
쿼리 파라미터 `action`으로 분기.

| action | 파라미터 | 설명 | 응답 데이터 |
|---|---|---|---|
| `adminData` | - | **관리자 화면이 쓰는 기본 조회.** 신청 목록 + 마감일을 한 번의 실행으로 함께 반환 | `{ applications: Application[], blocked: string[] }` |
| `list` | - | 전체 신청 목록 조회 (하위 호환용) | `Application[]` |
| `blocked` | - | 마감일 목록 조회 | `string[]` (YYYY-MM-DD 배열) |
| `lookup` | `name`, `empid` | 이름+사번으로 본인 신청 내역 조회 | `Application[]` |
| `status` | `id` | 신청 1건의 현재 상태만 조회 (완료 화면 폴링용) | `Application \| null` |

### 3.2 POST 요청 (`doPost`)
body는 JSON 문자열, `{ "action": "...", ...payload }` 형태.

| action | payload | 설명 |
|---|---|---|
| `submit` | `{ data: { name, empid, branch, roomtype, date, nights, memo } }` | 신규 신청 등록. 서버에서 `id`(UUID), `appNo`(4자리) 생성 후 `{id, appNo}` 반환 |
| `updateStatus` | `{ id, status }` | 상태 변경 (대기/승인/거절) |
| `updateReservation` | `{ id, reservationNo }` | 예약번호 기록 |
| `updateStatusBulk` | `{ ids: string[], status }` | 여러 건 상태 일괄 변경. 시트를 한 번만 읽고 status 열을 한 번의 `setValues`로 기록. `{ updated, notFound[] }` 반환 (최대 200건) |
| `deleteApplication` | `{ id }` | 신청 1건 삭제 (시트에서 행 제거) |
| `deleteApplicationsBulk` | `{ ids: string[] }` | 여러 건 삭제. 행 번호가 밀리지 않도록 **아래쪽 행부터** 지우고 연속 구간은 `deleteRows` 로 묶어 처리. `{ deleted, notFound[] }` 반환 (최대 200건) |
| `blockDate` | `{ date }` | 마감일 추가 (형식·접수기간·일~목요일 여부를 서버에서도 검증) |
| `unblockDate` | `{ date }` | 마감일 해제 |

**시트 읽기 원칙**: `getDataRange()` 는 쓰지 않는다. "내용이 있는 마지막 행/열"까지 통째로 가져오기 때문에,
한 열만 필요한 작업에서도 13개 열을 전부 실어 온다. 대신 `readRows(sheet, 열수)` / `readColumn(sheet, 열번호)` /
`findRowById(sheet, id)` 로 필요한 범위만 읽는다. 신청 200건 기준 작업 1회당 읽는 셀 수:

| 작업 | 이전 | 현재 |
|---|---|---|
| 신청 제출 | 2,615 | 200 |
| 일괄 승인 10건 | 2,626 | 211 |
| 예약번호 입력 | 2,626 | 201 |
| 관리자 새로고침 | 2,630 | 2,615 (표에 전 항목이 필요하므로 그대로) |

응답 형식(공통): `{ ok: true, data: ... }` 또는 `{ ok: false, error: "..." }`.
서버는 `LockService.getScriptLock()`으로 동시 쓰기 충돌을 방지함 (최대 30초 대기).

### 3.3 서버 측 검증 (`Code.gs`)
`submit`은 `validateSubmission()`을 통과해야만 기록된다. 검증 항목과 오류 코드:

| 오류 코드 | 조건 |
|---|---|
| `missing_name` / `name_too_long` | 이름 없음 / 20자 초과 |
| `invalid_empid` | 숫자 1~6자리가 아님 |
| `invalid_branch` / `invalid_roomtype` / `invalid_nights` | 정해진 선택지 밖의 값 |
| `invalid_date` | `YYYY-MM-DD` 형식이 아니거나 달력에 없는 날짜(예: 2026-11-31) |
| `date_out_of_range` | 접수 기간(2026-10-01~12-31) 밖 |
| `date_not_allowed_weekday` | 체크인이 금·토요일 |
| `stay_not_available` | 묵는 밤 중에 금·토·공휴일·마감일이 포함됨 |
| `memo_too_long` | 요청사항 500자 초과 |
| `invalid_status` | 대기/승인/거절 외의 상태값 |

`apply.html`의 `ERROR_MESSAGES` 맵이 이 코드를 한국어 안내 문구로 바꿔 보여준다.
규칙 상수(`RANGE_START`/`RANGE_END`/`HOLIDAYS`/`BRANCHES`/`ROOMTYPES`)는 `Code.gs`와
`apply.html` 양쪽에 있으므로 **연도나 선택지가 바뀌면 두 파일을 함께 고쳐야 한다.**

## 4. 비즈니스 규칙 (클라이언트 검증 로직, apply.html 내부)

- **선택 가능 요일**: 일(0)~목(4)요일만. 금/토 완전 비활성화.
- **선택 가능 기간**: 2026-10-01 ~ 2026-12-31. 캘린더 이전/다음 달 버튼도 이 범위를 벗어나지 못하도록 비활성화.
- **2026년 공휴일 하드코딩 목록** (`HOLIDAYS_2026` Set, apply.html 상단): 신정, 설 연휴, 삼일절 대체공휴일, 근로자의날, 어린이날, 부처님오신날(대체), 지방선거 임시공휴일(6/3), 현충일, 제헌절, 광복절(대체), 추석 연휴, 개천절(대체), 한글날, 크리스마스 — 총 22일. **연도가 바뀌면 이 목록을 갱신해야 함.**
- **관리자 마감일**: 관리자가 특정 날짜를 추가로 막을 수 있음 (일~목요일만 마감 가능, 주말/공휴일은 이미 막혀있으므로 의미 없음).
- **박수(1박2일/2박3일) 검증**: 체크인일 기준으로 묵는 날짜들 중 금/토/공휴일/마감일이 포함되면 해당 박수를 선택 불가 처리. 예: 목요일 체크인 + 2박3일 → 금요일 밤을 포함하므로 차단됨. (`isStayValid()` 함수)
- **인원**: 전 지점 2인 기준 고정, 입력 필드 없음.
- **사번**: 숫자만, 최대 6자리로 자동 제한.
- **신청번호**: 4자리 숫자, 서버에서 기존 값과 중복되지 않는 값을 최대 50회 랜덤 시도로 생성. (동시 제출 시 극히 낮은 확률로 경합 가능 — `LockService`로 어느 정도 완화됨)

## 5. 기능 목록

### 5.1 직원용 화면 (`apply.html`)
- 탭 전환: "신청하기" / "내 신청 조회"
- 신청 폼: 이름, 사번, 지점, 룸타입, 이용일(커스텀 캘린더 팝업), 박수, 요청사항(선택)
- 제출 시 서버 통신 실패에 대비한 에러 배너 표시
- 제출 완료 화면: 신청번호/신청자/지점/룸타입/이용일/인원/상태 요약 카드. 상태는 10초마다 폴링하여 실시간처럼 갱신(승인/거절 시 뱃지 색 자동 변경). 폴링은 **승인/거절이 확정되면 중단**되고, 탭이 가려진 동안에는 호출하지 않으며, 최대 30분 뒤 자동 종료된다(탭으로 돌아오면 즉시 1회 확인)
- 내 신청 조회: 이름+사번으로 검색, 신청번호/룸타입/이용일/박수/상태/예약번호(있는 경우) 카드 목록으로 표시

### 5.2 관리자 화면 (`admin.html`)
- 상단 통계: 전체/대기/승인/거절 건수
- 마감일 관리: 날짜 추가/삭제 (칩 형태 목록)
- 검색(이름/사번/신청번호), 날짜 필터, 상태 필터 탭
- 목록 테이블: 신청번호, 이름, 사번, 지점, 룸타입, 이용일, 인원, 박수, 요청사항, 상태(드롭다운으로 즉시 변경), **예약번호(직접 입력 가능, blur 시 자동 저장)**
- 체크박스 다중 선택 → 일괄 승인/거절 (`updateStatusBulk` 요청 1건으로 처리)
- **삭제**: 행마다 삭제 버튼, 다중 선택 시 일괄 삭제. 확인 창을 거치고, 다른 쓰기와 같이 화면에서 먼저 지운 뒤 요청을 보내며 실패하면 원래 자리로 되돌린다.
  **시트에서 행을 실제로 지우므로 API 로는 복구되지 않는다** — 되돌리려면 구글 시트의 파일 > 버전 기록을 써야 한다(확인 창에도 같은 안내가 있다).
- CSV(엑셀) 다운로드 (신청번호 포함 전체 컬럼)
- 20초 자동 새로고침 + 수동 새로고침 버튼. **표 안을 편집 중(입력칸/드롭다운에 포커스)일 때는
  다시 그리지 않고 보류했다가, 포커스가 빠질 때 반영한다**(`requestRender`/`pendingRender`).
  통계 숫자는 보류 중에도 계속 갱신된다.

## 6. 디자인 시스템
- **컬러 팔레트** (CSS 변수, 두 파일에 동일하게 정의됨):
  - `--bg: #FFF8F3` (연한 크림 배경)
  - `--panel: #FFFFFF` (카드/패널)
  - `--ink: #262421` / `--ink-soft: #78716C` (텍스트)
  - `--gold: #F2631A` / `--gold-deep: #D94E0C` (포인트 오렌지 — 브랜드 컬러, 현대해상 로고 톤 참고)
  - `--green: #16A34A`(승인) / `--amber: #CA8A04`(대기) / `--red: #DC2626`(거절) — 상태 뱃지는 모두 단색 채움 + 흰 글씨 스타일
  - 다크모드 미지원 (의도적으로 제거함 — 기기 다크모드 설정과 무관하게 항상 라이트 크림톤 고정)
- **폰트**: 제목/헤딩류는 `Gowun Dodum`(둥글고 가독성 좋은 손글씨 느낌), 본문은 `Gothic A1`. Google Fonts CDN 사용.
  - **굵기는 400/700 두 가지만 로드한다.** 한글 폰트는 굵기 하나가 곧 파일 하나라, 쓰지 않는 굵기를 받으면 그만큼 그대로 낭비된다.
    예전에는 400/500/700/800 네 가지를 받으면서 정작 CSS 는 대부분 `600`(로드하지 않는 굵기 → 브라우저가 700 으로 대체)을 쓰고
    800 은 한 번도 쓰지 않았다. 지금은 CSS 에 선언된 굵기와 로드하는 굵기가 일치한다.
  - **CSS 에 400/700 외의 `font-weight` 를 새로 쓰지 말 것.** 쓰려면 Google Fonts URL 의 `wght@400;700` 에 그 굵기를 함께 추가해야 하고,
    추가하지 않으면 가장 가까운 굵기로 대체 렌더링되어 의도한 두께가 나오지 않는다.
  - 더 줄이고 싶다면: `Gowun Dodum`(제목 전용, 한 패밀리 통째)을 빼고 제목도 `Gothic A1 700` 으로 처리하면 폰트 로드가 한 번 더 크게 준다. 다만 디자인이 달라진다.

## 7. 알려진 이슈 / 주의사항
0. **체감 속도 처리 방식**: 요청당 1~3초는 없앨 수 없으므로, 쓰기 작업은 **화면을 먼저 바꾸고 요청은 뒤에서 보낸다**(낙관적 반영).
   실패하면 원래 상태로 되돌리고 안내한다 — 상태 드롭다운, 예약번호 입력, 일괄 승인/거절이 모두 이 방식이다.
   신청 제출은 서버가 발급하는 신청번호를 기다려야 하므로, 완료 화면으로 즉시 전환하되
   **서버 확인 전에는 "접수되었습니다"라고 표시하지 않는다**(제목은 "접수하고 있습니다", 신청번호는 "발급 중...", 상태는 "접수 중").
   확인되면 문구가 바뀌고, 실패하면 완료 화면을 걷어내고 폼으로 되돌린다. 접수되지 않은 건을 접수된 것처럼 보이게 하지 않기 위한 것이다.
1. **Apps Script 응답 속도**: 무료 서비스 특성상 요청당 1~3초 정도 소요됨(컨테이너 기동 + `/exec` → `script.googleusercontent.com` 리다이렉트로 왕복이 2회). 줄일 수 있는 건 **요청 횟수**뿐이라, 관리자 새로고침은 `adminData` 1건으로 합쳤고, 마감일 추가/해제는 전체 재조회 대신 로컬 상태만 갱신하며, 중복 호출은 진행 중인 요청에 합류시킨다(`inFlightLoad`). 받은 데이터가 이전과 같으면 표를 다시 그리지 않는다. 그래도 요청당 1~3초는 남으므로, 완전한 즉각 반응을 원하면 별도의 정식 백엔드(Node.js+DB 등)로 이전 필요.
2. **테스트 환경 주의**: `file:///` 로컬 파일이나 Claude 앱 내장 미리보기(샌드박스)에서 열면 외부 fetch 요청이 차단되어 정상 동작하지 않음. 반드시 실제 https 호스팅 환경(GitHub Pages 등)에서 테스트할 것.
3. **파일명 주의**: GitHub Pages는 파일명 그대로 URL 경로가 됨. `apply.html`/`admin.html` 이름이 뒤바뀌면 엉뚱한 화면이 뜨므로 업로드 시 파일명 재확인 필요.
4. ~~**서버 측 검증 미비**~~ → 해결됨. `submit`/`blockDate`/`updateStatus`가 서버에서도 요일·기간·공휴일·마감일·선택지를 검증함(3.3 참고). 단, 규칙 상수가 `Code.gs`와 `apply.html`에 이중으로 존재하므로 갱신 시 두 곳을 모두 고쳐야 함.
5. **신청번호 동시성**: 극히 드물지만 동시에 같은 4자리 번호가 발급될 가능성이 이론상 존재 (LockService로 완화되지만 100% 보장은 아님).
6. **공휴일 목록 하드코딩**: 연도가 바뀌면 `apply.html`의 `HOLIDAYS_2026`과 `Code.gs`의 `HOLIDAYS`를 **둘 다** 수동으로 갱신해야 함. 자동 공휴일 API 연동은 되어있지 않음. 참고로 현재 접수 기간(10~12월) 안에서 실제로 추가 차단되는 공휴일은 **10월 5일(월, 개천절 대체) 하루뿐**이다 — 10/3·10/9·12/25는 어차피 토·금요일이라 이미 막혀 있다.
7. **관리자 화면 접근 제한 없음**: URL만 알면 누구나 열 수 있고, `list` API도 인증이 없음.
8. **본인 조회의 식별 강도**: `lookup`은 이름+사번만 맞으면 통과하므로, 남의 사번을 아는 사람은 그 사람의 신청 내역(지점·이용일·예약번호)을 볼 수 있음. 사내 한정 용도라 현재는 감수 중.

## 8. 향후 개선 아이디어 (선택)
- 서버 측 입력 검증 강화 (위 7-4 참고)
- 실시간성이 중요하다면 Firebase/Supabase 등 실시간 DB로 백엔드 교체
- 관리자 인증(비밀번호) 추가 — 현재는 URL만 알면 누구나 관리자 화면 접근 가능
- 엑셀 다운로드에 필터 상태 반영 옵션 세분화
- 신청 취소/수정 기능 (현재는 신청 후 직원이 직접 수정 불가)

---
*최종 갱신: 이 문서는 대화 히스토리를 기반으로 작성되었습니다. 코드와 실제 배포 상태가 이 문서와 다를 경우 코드가 우선합니다.*
